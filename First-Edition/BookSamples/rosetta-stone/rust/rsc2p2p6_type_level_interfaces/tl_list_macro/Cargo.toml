[package]
name = "tl_list_macro"
version = "0.1.0"
edition = "2021"

[lib]
proc-macro = true

[dependencies]
quote = "1.0"   # For generating Rust code
syn = "2.0"     # For parsing input tokens
tl_list_lib = { path = "../tl_list_lib" }
